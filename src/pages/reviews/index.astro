---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import FormattedDate from '../../components/FormattedDate.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';

const reviews = (await getCollection('reviews')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title="Reviews" description="Reviews and thoughts" />
		<style>
			main {
				width: 960px;
			}
			.reviews-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
				gap: 2rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			.review-card {
				display: flex;
				flex-direction: column;
				transition: transform 0.2s ease;
			}
			.review-card:hover {
				transform: translateY(-4px);
			}
			.review-poster {
				position: relative;
				width: 100%;
				aspect-ratio: 2/3;
				border-radius: 8px;
				overflow: hidden;
				background: var(--gray-light);
			}
			.review-poster img {
				width: 100%;
				height: 100%;
				object-fit: cover;
				transition: transform 0.3s ease;
			}
			.review-card:hover .review-poster img {
				transform: scale(1.03);
			}
			.review-info {
				margin-top: 0.75rem;
				text-align: center;
			}
			.review-title {
				margin: 0 0 0.25rem 0;
				font-size: 1.1rem;
				line-height: 1.3;
				color: rgb(var(--black));
			}
			.review-meta {
				display: flex;
				justify-content: center;
				gap: 1rem;
				font-size: 0.85rem;
				color: var(--gray);
				margin-bottom: 0.25rem;
			}
			.review-rating {
				color: #eab308;
				font-weight: 600;
			}
			.review-date {
				color: var(--gray);
				font-size: 0.8rem;
			}
			.review-card a {
				text-decoration: none;
				color: inherit;
			}
			.review-card a:hover .review-title {
				color: var(--accent);
			}
			@media (max-width: 720px) {
				.reviews-grid {
					grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
					gap: 1.5rem;
				}
			}
		</style>
	</head>
	<body>
		<Header />
		<main>
			<section>
				<h1 style="text-align: center; margin-bottom: 2rem;">Reviews</h1>
				<ul class="movies-grid">
					{
						reviews.map((review) => (
							<li class="movie-card">
								<a href={`/reviews/${review.id}/`}>
									<div class="movie-poster">
										{review.data.heroImage && (
											<Image width={400} height={600} src={review.data.heroImage} alt={review.data.title} />
										)}
									</div>
									<div class="movie-info">
										<h3 class="movie-title">{review.data.title}</h3>
										<div class="movie-meta">
											{review.data.year && <span>{review.data.year}</span>}
											{review.data.rating && <span class="movie-rating">★ {review.data.rating}/10</span>}
										</div>
										<p class="movie-date"><FormattedDate date={review.data.pubDate} /></p>
									</div>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
		<Footer />
	</body>
</html>